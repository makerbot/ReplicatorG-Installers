<?xml version="1.0" encoding="iso-8859-1" standalone="yes" ?>

<!-- 
    A sample installation file.
    Use it as a base for your own installers :-)
    
    To compile it :
    - go in the bin directory where you installed IzPack
    - call "compile ../sample/install.xml -b ../sample"
-->

<installation version="1.0">

    <!-- 
        The info section.
        The meaning of the tags should be natural ...
    -->
    <info>
        <appname>ReplicatorG</appname>
        <appversion>30</appversion>
        <authors>
            <author name="Makerbot Industries" email=""/>
            <author name="The 3D printing community!" email=""/>
        </authors>
        <url>http://www.replicat.org</url>
    </info>

    <!-- 
        The gui preferences indication.
        Sets the installer window to 640x480. It will not be able to change the size.
    -->
    <guiprefs width="640" height="480" resizable="yes"/>
    
    <!-- This allows us to add shortcuts (on windows only? not sure.) -->
    <native type="izpack" name="ShellLink.dll"/>

    <!-- 
        The locale section.
        Asks here to include the English langpacks.
    -->
    <locale>
        <langpack iso3="eng"/>
    </locale>

    <!-- 
        The resources section.
        The ids must be these ones if you want to use the LicencePanel and/or the InfoPanel.
    -->
    <resources>
        <!--res id="InfoPanel.info" src="dist/windows/replicatorg-0028/readme.txt"/-->
        <!--res id="shortcutSpec.xml" src="shortcutSpec.xml"/-->
    </resources>

    <!-- 
        The panels section.
        We indicate here which panels we want to use. The order will be respected.
    -->
    <panels>
        <panel classname="HelloPanel"/>
        <panel classname="InfoPanel"/>
        <panel classname="TargetPanel"/>
        <panel classname="PacksPanel"/>
        <panel classname="InstallPanel"/>
        <panel classname="ShortcutPanel"/>
        <panel classname="FinishPanel"/>
    </panels>

    <!-- 
        The packs section.
        We specify here our packs.
    -->
    <packs>
        <pack name="Base" required="yes">
            <description>The base files</description>
            <file src="dist" targetdir="$INSTALL_PATH"/>
            <!-- The file will be parsed -->
        </pack>
    </packs>

</installation>
